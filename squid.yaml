manifestVersion: subsquid.io/v0.1
name: zenlink-bifrost-test-squid
version: 2
description: |-
  Zenlink Bifrost(Kusama) Squid
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server"]
